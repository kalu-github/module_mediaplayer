<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 选中&在播 -->
    <item android:color="@android:color/white" android:state_activated="true" android:state_hovered="true" />
    <!-- 在播 -->
    <item android:color="@color/module_mediaplayer_color_ff5a07" android:state_selected="true" />
    <!-- 选中 -->
    <item android:color="@android:color/white" android:state_activated="true" />
    <!-- 默认 -->
    <item android:color="@android:color/white" />

</selector>